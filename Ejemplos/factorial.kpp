int factorial(int n) {
	int x, y;

	if ((n) == 1) {
		x = 1;
	}
	else {
		y = n - 1;

		x = (n * factorial(y));
	}

	return x;
}

main {
	int j, j1, k[10];

	j1 = input(int);

	j = (factorial(j1));

	print(j);
}